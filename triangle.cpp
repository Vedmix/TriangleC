#include <iostream>

using namespace std;

int main(){
    double ax, ay, bx, by, cx, cy;
    cout << "Enter the coordinates of the points" << "\n";
    cout << "Point A: ";
    cin >> ax >> ay;
    cout << "Point B: ";
    cin >> bx >> by;
    cout << "Point C: ";
    cin >> cx >> cy;

    return 0;
}
